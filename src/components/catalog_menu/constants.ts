import { API, CATALOG } from 'constants/';

const REQUESTED_CATEGORY = 'Каталог';
export const CATALOG_ICON = 'Catalog';
export const TITLE = 'Каталог';

export const SEARCH_API = `${API}/${CATALOG}${REQUESTED_CATEGORY}`;
