const API_HOSTS = {
  imageComponent: {
    localAPIHost: 'localhost',
    nasaAPIHost: 'api.nasa.gov',
  },
};
// export const IMAGE_LOAD_DOMAINS = Object.keys(API_HOSTS.imageComponent).map(itemId => API_HOSTS.imageComponent[itemId]);

export const ENTER_BUTTON = 'Enter';
export const BACKSPACE_BUTTON = 'Backspace';
export const EMPTY_STRING = '';
